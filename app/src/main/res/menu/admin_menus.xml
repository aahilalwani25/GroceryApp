<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:enabled="true"
        android:checkableBehavior="single"

        >
        <item android:title="Add items"
            android:id="@+id/add_items_admin"
            android:icon="@drawable/ic_add_item"
            android:iconTint="@color/black"
            />

    </group>

    <group
        android:enabled="true"
        android:checkableBehavior="single"
        >
        <item android:title="See orders"
            android:id="@+id/see_orders"
            android:icon="@drawable/ic_add_item"
            android:iconTint="@color/black"
            />
        <item android:title="Customer Feedback"
            android:id="@+id/customer_feedback"
            android:icon="@drawable/ic_baseline_feedback_24"
            />


    </group>

</menu>